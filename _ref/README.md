# Reference sources

This directory does not vendor clones. Local dump/parity use ExifTool **13.59** at `C:/projects/projects.rust.cg/vfx.ref/exiftool` (or `EXIFTOOL_LIB` / `EXIFTOOL_PERL`).

Upstream repositories formerly checked out here as git submodules:

- ExifTool: https://github.com/exiftool/exiftool
- exif-rs (kamadak): https://github.com/kamadak/exif-rs
